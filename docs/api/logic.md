# Logic

Differentiable fuzzy-logic gates — Boolean operations on soft truth values in
`[0, 1]`, as functions and as parameter-free `nn.Module` neurons. The radial-basis
activation `radbas` lives in [Ops](ops.md).

::: polyweave.logic
